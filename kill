toKill = [x, x, x, x ]

for proc in toKill
	r = computer.close_program(proc)
	if r == 1 then
		print("Process " + proc + " killed")
	else if r == 0 then
		print("Process " + proc + " cannot be found")
	else
		print(r)
	end if
end for
